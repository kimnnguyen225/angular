export class Student {
  name: string;
  favColor: string;
  favNumber: number;
    constructor(name: string, favColor: string, favNumber: number) {
      this.name=name;
      this.favColor=favColor;
      this.favNumber=favNumber;
    }
}
